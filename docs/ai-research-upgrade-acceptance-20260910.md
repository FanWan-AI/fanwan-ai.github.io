# 研究模块升级验收记录

## 范围和 Git 边界

本次仅升级论文中心、模型雷达及其生成、发布、测试和部署连接。并行实施后由主代理整合验收。远端自动新闻更新已 fast-forward 至 `e1a6246a`；没有重写远端历史。

排除 `assets/ai-startup/startup.js`、`data/ai/startup/cases/cursor-workflow-wedge.json`；其他代理修改不纳入提交。`.tmp`、`.strata`、旧 draft、缓存归档和运行日志不提交。本次公共数据只增加审核后的当期与索引、更新配套别名和合并分类库，不覆盖完整历史缓存。

## 内容和功能结果

- 论文：2026-09-10 版包含两篇中文摘要级研究解读；不再展示讨论帖、英文复制正文或默认 50/30 分。
- 模型：2026-09-10 版包含 LangGraph 与 XHToken/Spark-X2.5-4B，提供中文介绍、用途、关注理由、边界、建议验证与发布者节选。
- 原模型分类库完整保留，GitHub 901 → 902、HF 364 → 365；逐项核对原 canonical_id 未丢失。
- 初始模型试跑中的通用交易引擎和健身应用未提升到正式精选。LangGraph 以官方 README 人工编写并通过同一验证器；Spark 来自真实 DeepSeek 输出，验证建议经人工修订为隔离环境与合成数据。没有实际执行这些项目或模型。
- 论文真实调用 2 次，模型真实调用 4 次；失败／拒绝的输出未当作正常精选发布。本轮后续复核未继续调用付费模型。

## 验证证据

- `npm run research:test`：72 项通过，涵盖内容契约、来源、预算、去重、失败保留、文件回滚、页面脚本与工作流 YAML。
- `npm run startup:test`：18 项通过；首次失败是 sparse checkout 缺少未修改的公开 fixture，补齐指定文件后通过，未修改创业实现。
- `npm run research:verify`：校验论文哈希、当期归档、模型双源快照与别名、来源正文及身份。
- 浏览器：桌面 1440 与手机 390 宽度；论文搜索、空结果、日期切换、详情展开、经典 NeRF 轨迹按需加载；语言切换；模型正文、HF 365 项分类库与任务分类展开。文档宽度均不超过视口。英文界面明确中文解读状态。
- 浏览器发现旧 `setDataSourceLabel` 缺失导致成功内容误报失败；已保护可选调用并增加回归测试。手机日期控件避免压缩，单卡独占行，精选视图不显示无意义排序控件。
- 发布打包增加新页面、脚本、样式和双源快照必需文件检查；单独离线 CI 无密钥运行。

## 调度和边界

论文每日北京时间 08:00、模型每日 10:30 触发。Actions 排队及数据源状况会影响实际完成时间，失败保留上一版并报告失败，不把日期改成今天冒充更新。

本次没有全文核验全部经典轨迹，也没有独立实测模型能力。历史分类库沿用旧说明，不能视为已按新版来源契约全量复审。当前候选发现是有预算的筛选，不保证覆盖所有方向。单次验收不能证明连续多日生成稳定。

安装审计报告仍含旧构建工具链的依赖漏洞，本次没有执行破坏性 `audit fix --force` 或大版本迁移；新的静态阅读模块无浏览器端密钥。不要将本记录理解为全站安全审计。

最终线上结果以本次提交对应的 **Research modules checks** 和 **Deploy static site to GitHub Pages** 运行记录及公开页面为准。

## 发布确认

- 功能提交 `bfbdeda2` 已推送 main；其后自动分类提交 `de162edb` 已 fast-forward 同步，本次没有 force push、reset 或覆盖他人改动。
- [Research modules checks](https://github.com/FanWan-AI/fanwan-ai.github.io/actions/runs/34494446443)：成功。
- [Pages 部署](https://github.com/FanWan-AI/fanwan-ai.github.io/actions/runs/34494446592)：成功。
- 线上两张 HTML、新阅读 CSS 和两份最新 JSON 均返回 HTTP 200；内容匹配升级版，日期 2026-09-10，论文 2 篇、模型双源各 1 项。
- 本地 sparse checkout 补充本次阅读资产、测试、论文数据和模型必要索引／近期数据，避免后续 pull 再次移除本地运行依赖；仍未下载全站的大体积音频与无关资产。旧打包目录保留为 `.tmp/pages-upload-before-research-20260910`，没有删除。
