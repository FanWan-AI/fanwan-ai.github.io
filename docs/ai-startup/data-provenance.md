# 公开数据来源登记（provenance）

记录 `data/ai/startup/` 下每份数据的产生方式，供验收与回滚时判断可信度。上线后由流水线产生的数据以 `pipeline_version` 与 Git 历史为准。

| 文件 | 产生方式 | 说明 |
| --- | --- | --- |
| `opportunities/latest.json` 及 `archive/2026-09-08.json`（2026-09-08 期，1 条机会） | GitHub 真实采集、生成与复核流水线 | 运行 `34195822255`，提交 `c6d19dd4`；9 月 9 日未通过独立来源检查，仍保留该期。通过机器检查不等于所有商业判断已获实证 |
| 历史 `opportunities/latest.json`（2026-09-04 期，5 条机会） | 人工研究并编辑的首期内容 | 不是自动流水线产物；保存在 Git 历史中，9 月 8 日被真实产物替换 |
| `cases/cursor-workflow-wedge.json`（5 节 / 14 来源） | 人工研究并编辑的首期案例 | 作为 ADR-003 所述"黄金样本"的初版；其论证结构是后续周案例的质量参照，不是事实核对基准 |
| `cases/index.json` | 随种子案例手工建立 | 首次 `promote-case` 后由流水线维护 |

更正（2026-09-08）：不能仅凭文件与 fixture 同构推断写作过程。9 月 4 日版本应登记为人工研究并编辑的首期内容，而非真实流水线产物；自动化测试通过也不等于完成事实核查。本机与 GitHub 实际均已配置 DeepSeek 密钥。核心来源本轮重新复核，见 [验收日志](./phase-log-20260908.md)。

## 处置原则

- 首期内容中的事实以其引用的原始文献为依据，不能以本 JSON 或模型输出自证。
- 只有完整生成、复核与发布检查通过后才更新 `latest.json`；dry-run 不调用完整模型复核，不能代替发布验收。
- 任何手工修订都必须带 `updated_at` + `revision_note`（编辑政策 §6）。
